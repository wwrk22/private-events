<% content_for :head do %>
  <title><%= @user.name %></title>
<% end %>

<div>
  <h3><%= "Hello, #{@user.name}!" %></h3>
</div>

<div>
  <h4>Events I'm hosting</h4>
  <ul>
    <% @hosted_events.each do |e| %>
      <!-- <li><%= "#{e.name} @#{e.location}, on #{e.event_date}" %></li> -->
    <% end %>
  </ul>
</div>
